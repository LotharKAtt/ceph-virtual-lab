classes:
- system.linux.system.repo.ubuntu
- system.salt.master.pkg
- system.salt.minion.ca.salt_master
- system.salt.master.api
- system.reclass.storage.salt
- system.reclass.storage.system.ceph_monitor_cluster
- cluster.virtual-ceph-lab.ceph
- cluster.virtual-ceph-lab.infra
parameters:
  _param:
    salt_master_base_environment: prd
    reclass_data_repository: "https://gerrit.mcp.mirantis.net/salt-models/mcp-virtual-lab"
    reclass_data_revision: master
    reclass_config_master: ${_param:infra_config_deploy_address}
    single_address: ${_param:infra_config_address}
    linux_system_codename: xenial
    salt_api_password_hash: "$6$WV0P1shnoDh2gI/Z$22/Bcd7ffMv0jDlFpT63cAU4PiXHz9pjXwngToKwqAsgoeK4HNR3PiKaushjxp3JsQ8hNoJmAC6TxzVqfV8WH/"
    salt_master_host: ${_param:infra_config_deploy_address}
  salt:
    master:
      reactor:
        reclass/minion/classify:
        - salt://reclass/reactor/node_register.sls
  reclass:
    storage:
      class_mapping:
        common_node:
          expression: all
          node_param:
            single_address:
              value_template: <<node_control_ip>>
            linux_system_codename:
              value_template: <<node_os>>
            salt_master_host:
              value_template: <<node_master_ip>>
        infra_config:
          expression: <<node_hostname>>__startswith__cfg
          cluster_param:
            infra_config_address:
              value_template: <<node_control_ip>>
            infra_config_deploy_address:
              value_template: <<node_deploy_ip>>
            cluster_domain:
              value_template: <<node_domain>>
        ceph_osd_node01:
          expression: <<node_hostname>>__equals__osd0
          cluster_param:
            ceph_osd_node01_address:
              value_template: <<node_osd_ip>>
          value_template:
            - cluster.<<node_cluster>>.ceph.osd
        ceph_osd_node02:
          name: ${_param:ceph_osd_node02_hostname}
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.ceph.osd
          params:
            salt_master_host: ${_param:infra_config_deploy_address}
            linux_system_codename: xenial
            single_address: ${_param:ceph_osd_node02_address}
        ceph_osd_node03:
          name: ${_param:ceph_osd_node03_hostname}
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.ceph.osd
          params:
            salt_master_host: ${_param:infra_config_deploy_address}
            linux_system_codename: xenial
            single_address: ${_param:ceph_osd_node03_address}
        ceph_cfs_node01:
          name: ${_param:ceph_cfs_node01_hostname}
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.ceph.cfs
          params:
            salt_master_host: ${_param:infra_config_deploy_address}
            linux_system_codename: xenial
            single_address: ${_param:ceph_cfs_node01_address}
        ceph_monitor_node01:
          params:
            linux_system_codename: xenial
            keepalived_vip_priority: 100
        ceph_monitor_node02:
          params:
            linux_system_codename: xenial
            keepalived_vip_priority: 101
        ceph_monitor_node03:
          params:
            linux_system_codename: xenial
            keepalived_vip_priority: 102
